export enum UserRole {
    Admin = 'admin',
    Student = 'student',
    Faculty = 'faculty',
    Staff = 'staff'
}

export enum MessageStatus {
    Unread = 'unread',
    Read = 'read',
    Delivered = 'delivered',
    Opened = 'opened'
}
