export interface AssignmentFile {
    fileName: string;
    fileUrl: string;
    fileSize: number;
}

export interface SubmissionFile {
    fileName: string;
    fileUrl: string;
    fileSize: number;
}
